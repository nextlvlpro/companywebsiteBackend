const mongoose = require('mongoose')

    const retailerpayoutSchema = new mongoose.Schema({
        

    })

const retailerpayouts = mongoose.model('retailerpayouts', retailerpayoutSchema)
module.exports = retailerpayouts